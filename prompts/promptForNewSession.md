---
description: Generate a prompt for continuing work in a new session
argument-hint: "[context/instructions...]"
---
Give me a prompt I can use in a new session to address the following: If I ask specifically for multiple prompts, then deliver the prompts in a format that each can be run in its own separate session: $@.